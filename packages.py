# a package is a qualified folder with one or more scripts.
# a folder is termed as qualified package if it has (_ _) init (_ _).py
# a folder with more packages is called a library
# python is termed as modular coz it has multiple modules.